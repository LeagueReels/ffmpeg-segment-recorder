# FFmpeg Segment Recorder

Rolling-buffer desktop recorder using FFmpeg (gdigrab + segment muxer).

## Features

- Rolling `.ts` segment buffer
- Automatic pruning
- Optional DirectShow audio capture
- No GUI — lightweight CLI tool
- Windows support (gdigrab)
- MIT licensed

## Installation

```bash
pip install imageio-ffmpeg
